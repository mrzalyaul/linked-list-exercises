/**
 * Reverses the linked list so that the last node becomes the first node, and vice versa.
 * This operation modifies the list in place and returns the new head of the list.
 * Example: Given 1 -> 2 -> 3 -> 4, after reversal it becomes 4 -> 3 -> 2 -> 1
 *
 * @param {LinkedListNode} head - The current head of the linked list.
 * @returns {LinkedListNode} The new head of the list after reversal.
 */
function reverseLinkedList(head) {
    // Implementation goes here
}

module.exports = reverseLinkedList;