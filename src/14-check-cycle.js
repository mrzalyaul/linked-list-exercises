/**
 * Checks if the linked list has a cycle, meaning if a node's next points back to a previous node in the list, forming a loop.
 * Returns true if a cycle is found, otherwise false.
 * Example: Given a list that forms a cycle, it returns true. If the list does not form a cycle, it returns false.
 * Checks if the linked list has a cycle.
 * Example: Given 1 -> 2 -> 3 -> 4 -> 2, it returns true because the last node points back to the second node.
 * Given 1 -> 2 -> 3 -> 4, it returns false because there is no cycle.
 *
 * @param {Node} head - The head of the linked list.
 * @returns {boolean} True if the list has a cycle, otherwise false.
 */
function checkCycle(head) {
    // Implementation goes here
}

module.exports = checkCycle;