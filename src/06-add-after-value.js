/**
 * Inserts a new element after the first occurrence of a specific value in the linked list.
 * If the value is not found in the list, no action is taken.
 * Example: Given 1 -> 2 -> 3 and you add 4 after 3, it becomes 1 -> 2 -> 3 -> 4
 *
 * @param {Node} head - The head of the linked list.
 * @param {*} newValue - The value to insert.
 * @param {*} afterValue - The value after which the new value should be inserted.
 * @returns {Node} The head of the list.
 */
function addAfterValue(head, newValue, afterValue) {
    // Implementation goes here
}

module.exports = addAfterValue;