/**
 * Removes all duplicate values from the linked list, leaving only distinct values.
 * Example: Given 1 -> 2 -> 2 -> 3 -> 3 -> null, the function modifies it to 1 -> 2 -> 3 -> null.
 *
 * @param {LinkedListNode} head - The head of the linked list.
 * @returns {LinkedListNode} The head of the modified list.
 */
function removeDuplicates(head) {
    // Implementation goes here
}

module.exports = removeDuplicates;