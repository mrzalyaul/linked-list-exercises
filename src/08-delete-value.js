/**
 * Deletes the first occurrence of a specific value from the linked list.
 * If the value is not found in the list, no action is taken.
 * Example: Given 1 -> 2 -> 3 -> 2 -> 4 and you delete 2, it becomes 1 -> 3 -> 2 -> 4
 *
 * @param {Node} head - The head of the linked list.
 * @param {*} value - The value to delete.
 * @returns {Node} The head of the list, which may change if the first element is deleted.
 */
function deleteValue(head, value) {
    // Implementation goes here
}

module.exports = deleteValue;